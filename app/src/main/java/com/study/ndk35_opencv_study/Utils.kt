package com.study.ndk35_opencv_study

import android.content.Context
import androidx.camera.core.ImageProxy

class Utils {

    companion object {
        fun getDataFromImage(image: ImageProxy): ByteArray {
            //TODO:
            return ByteArray(0)
        }

        fun copyAsset2Dir(contest: Context, name: String): String {
            //TODO:
            return ""
        }
    }
}